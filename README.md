# Template for nestjs microservice container
This is a template to get started for the below stack
NestJS, TypeORM, Postgres, gRPC microservice

Installation steps
1. Clone the repo
2. Run the command from root of the project ```docker-compose up -d```
